[package]
name = "oxyn-query"
description = "Analyse des requêtes : dialectes, classification d'intention, formatage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
oxyn-core.workspace = true
serde.workspace = true
sqlparser.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
criterion.workspace = true
rstest.workspace = true

# `harness = false` : criterion fournit son propre `main`. Sans cela, le harnais
# de test de libtest s'installe et les arguments de criterion sont refusés.
[[bench]]
name    = "analysis"
harness = false
